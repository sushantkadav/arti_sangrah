import 'package:flutter/material.dart';

class GranthavaliList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Granthavali")),
      body: const Center(
        child: Text('Granthavali List'),
      ),
    );
  }
}
