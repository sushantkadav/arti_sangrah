import 'package:flutter/material.dart';

class PoojaPath extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PoojaPath")),
      body: const Center(
        child: Text('Pooja Path'),
      ),
    );
  }
}
